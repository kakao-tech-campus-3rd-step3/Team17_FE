import { Layout } from '@/components/common/Layout';
import Header from '@/components/common/Header';

const Home = () => {
  return (
    <Layout>
      <Header />
    </Layout>
  );
};

export default Home;
